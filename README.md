# Lector de OBJs en OpenGL

Para compilar y correr el código fuente 

```shell
mkdir -p -- out/
g++ src/lector_obj.cpp -o out/lector_obj -lGL -lGLU -lglut -lGLEW
/out/lector_obj
```

or

```shell
chmod +x run.sh
./run.sh
```

